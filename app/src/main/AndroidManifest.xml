<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.potato.room_checkmate">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginLandingMenu" />
        <activity android:name=".RegLand" />
        <activity android:name=".RegProfile" />
        <activity android:name=".RoomTools" />
        <activity android:name=".CheckMatch" />
        <activity android:name=".UpdateProfile" />
        <activity android:name=".UserSearch" />
        <activity android:name=".RoomChat" />
        <activity android:name=".RoomCalendar"></activity>
    </application>

</manifest>